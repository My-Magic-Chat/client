import { IProps } from '../interface';
const data: Partial<IProps> = {
    children: 'Some Text here',
    type: 'button',
    theme: 'brand',
    fluid: false,
    disabled: false
};

export default {
    data,
};
