import { IProps } from '../interface';
const data: Partial<IProps> = {
    children: 'Text Here',
    type: 'button',
    theme: 'brand',
    fluid: false,
    disabled: false
};

export default {
    data,
};
