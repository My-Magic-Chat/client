import { IProps, ETarget } from '../interface';

const data: Partial<IProps> = {
    children: 'Some Text here',
    target: ETarget.PARENT,
    url: 'https://www.w3schools.com/tags/tag_a.asp'
};

export default {
    data,
};
